Use this, close terminal freely:
https://minerals-installation-inform-gateway.trycloudflare.com
Manage it with:
docker ps | grep cloudflared
docker stop cloudflared-supabase   # stop
docker start cloudflared-supabase  # start

// used to see the new cloudflare tunnel url after reboot in the /home/rence/Supabase-dock dir
docker logs cloudflared-supabase 2>&1 | grep -o 'https://[^ |]*trycloudflare.com' | tail -1 

